apiVersion: apps/v1
kind: Deployment
metadata:
  name: friend-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: friend-mongo
  template:
    metadata:
      labels:
        app: friend-mongo
    spec:
      containers:
        - name: friend-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: friend-mongo-srv
spec:
  selector:
    app: friend-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017